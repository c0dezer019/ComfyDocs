// Tailwind 4 discovers source files and the design theme from CSS. The
// semantic aliases live in theme.css; this file remains for tools that still
// look for a legacy config entry point.
const config = {};

export default config;
